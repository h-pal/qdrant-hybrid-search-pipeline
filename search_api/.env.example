# Search API Service Environment Variables

# Qdrant Vector Database (use Railway internal URL)
QDRANT_URL=http://qdrant.railway.internal:6333

# Note: Search API only needs this 1 variable
# Dense embeddings now use local BAAI/bge-small-en-v1.5 model (no API key needed)
# No S3 credentials required - we only query, not embed
